name: Terraform Validate (disabled)

on: []

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow disabled
        run: echo "terraform-validate workflow temporarily disabled"
